<footer role="contentinfo" class="w-full p-4 bg-gray-100 flex items-center justify-center space-x-2">
    <span class="font-mono text-xs">&copy;
        {{ meta.title }}
        {{ build.timestamp | dateToFormat('yyyy') }}</span>
</footer>